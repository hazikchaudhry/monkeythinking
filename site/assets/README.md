# assets

Drop images here — referenced from the site as `assets/filename.png`.

Placeholder boxes in each tab show the expected filename; swap them for
`<img src="assets/filename.png" alt="...">` in `index.html` once the real
image is in this folder.
